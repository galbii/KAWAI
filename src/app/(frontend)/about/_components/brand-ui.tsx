/**
 * The brand primitives now live in the shared library so every company /
 * heritage / recognition page reads with the same hero-grade UI as About.
 * Re-exported here for backward compatibility with the existing scene imports.
 */
export { BrandEyebrow, BrandCTA, BrandArrowLink } from '@/components/brand'
